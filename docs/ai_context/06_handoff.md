# Handoff

## Summary for the next packet
- The product workspace now covers `/brands`, `/products`, `/media-assets`, `/audience-segments`, and `/content-plans`.
- Dashboard links to the full product workspace set and still serves as the scope-selection hub.
- The repo is under git, the web build is green, and content-plan generation is live.

## Do next
- Move on to `stage4-export-monetization`.
- Reuse the current org/brand/product scope model, the generated content plans, and the existing DNA payloads.
- Keep the new content-plans flow aligned with the dashboard scope selection.

## Do not do
- Do not reopen the API content-plan generation packet unless a new issue appears.
- Do not remove the current product list/edit flow or the media/audience/content-plan screens.
- Do not expand jobs/admin yet.
